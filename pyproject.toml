[project]
name = "vm-factory"
version = "0.1.0"
description = "AI Worker Factory & Node Lifecycle Pipeline Engine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7.0",
    "pyyaml>=6.0.1",
    "typer>=0.12.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
